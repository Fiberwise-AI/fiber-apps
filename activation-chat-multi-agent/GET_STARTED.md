1. Install Fiberwise Util
 Github
2. Generate API Key in Settings
3. fw utils account add-config

fw-util account add-config   --name "test-account"    --api-key "{Insert API Key}" --base-url "http://localhost:5555" --set-default

4. fw-util install

O[e]